name: Mantieni Progetto Attivo

on:
  schedule:
    - cron: '0 0 */5 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keepalive → chiama Edge Function
        run: |
          curl -X POST "https://ruopxyprezzxoirfrjrm.supabase.co/functions/v1/send-apk-link" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE }}" \
            -d '{"type":"keepalive","email":"ingnichettigb@gmail.com"}'

